import React, { useState } from 'react';
import {
    Bell, X, Check, Trash2, Filter, ArrowLeft,
    Briefcase, MessageSquare, Star, AlertCircle,
    CheckCircle2, Clock, User, DollarSign
} from 'lucide-react';

const Notifications = () => {
    
};

export default Notifications;